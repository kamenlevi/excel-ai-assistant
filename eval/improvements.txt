When describing Excel functions, provide a concrete example or Excel-specific context.
Example: "The VLOOKUP function looks up a value in a table and returns a corresponding value from another column. For instance, =VLOOKUP(A2, B:C, 2, FALSE) looks up the value in cell A2 in the first column of the range B:C and returns the corresponding value in the second column."

When filtering data, use Office JS APIs to interact with the worksheet.
Example: 
```javascript
const sheet = workbook.worksheets.getActiveWorksheet();
const table = sheet.tables.getItem("Table1");
table.columns.getItem("Department").filter.apply({
  filterOn: Excel.FilterOn.custom,
  customFormula: "=Department=\"Engineering\""
});
await context.sync();
```

When inserting shapes, dynamically determine the position based on the worksheet's dimensions.
```javascript
const sheet = workbook.worksheets.getActiveWorksheet();
const usedRange = sheet.getUsedRange();
usedRange.load("address");
await context.sync();
const shape = sheet.shapes.addShape(Excel.ShapeType.oval, usedRange.address.width, 0, 50, 50);
await context.sync();
```

For page layout settings, generate code that fully addresses the request.
```javascript
const sheet = workbook.worksheets.getActiveWorksheet();
sheet.pageSetup.headerMargin = 0.25;
sheet.pageSetup.footerMargin = 0.25;
sheet.pageSetup.centerHeader = true;
sheet.pageSetup.centerFooter = true;
await context.sync();
```

When updating comments, use the correct range and best practices for handling comments.
```javascript
const sheet = workbook.worksheets.getActiveWorksheet();
const cell = sheet.getRange("A1");
cell.load("comment");
await context.sync();
const comment = cell.comment;
const currentDate = new Date().toISOString().split('T')[0];
if (comment) {
  comment.text = comment.text + " - " + currentDate;
} else {
  cell.addComment(currentDate);
}
await context.sync();
```

When copying comments, use the correct range and comparison.
```javascript
const sheet = workbook.worksheets.getActiveWorksheet();
const commentRange = sheet.getRange("A1");
commentRange.load("comment");
await context.sync();
const comment = commentRange.comment;
const targetRange = sheet.getRange("B2:B5");
targetRange.load("values");
await context.sync();
const values = targetRange.values;
const valueA1 = sheet.getRange("A1").values[0][0];
for (let i =
