When describing a function, provide a brief explanation and relevant examples without unnecessary code.
For VLOOKUP, explain it looks up a value in a table and returns a corresponding value from another column.
If a column does not exist, throw an error instead of creating a new column.
Example: 
```javascript
try { 
  const filter = sheet.getRange("Department").autoFilter;
} catch (e) { 
  throw new Error("Department column not found");
}
```
