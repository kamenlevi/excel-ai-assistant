To improve handling of conditional formatting requests, add:
```
if prompt includes "highlight" or "format", ensure code uses Office JS APIs for conditional formatting.
```

For accurate VLOOKUP function descriptions, include:
```
when describing VLOOKUP, focus on its purpose and parameters without providing unnecessary code examples.
```

To address edge cases in filtering, such as showing only Engineering departments:
```
use real Office JS APIs for filtering, like applyColumnFilter or applyFilters.
```

For data validation, like ensuring a column only accepts whole numbers between 1 and 100:
```
use dataValidation.rule with appropriate operator and formula to restrict input values.
```

To correctly position shapes, such as inserting a circle in the top-right corner:
```
calculate shape position based on worksheet content and size to ensure accurate placement.
```

For updating comments, like adding the current date to an existing comment:
```
check if comment exists before updating, and handle potential errors gracefully.
```

To copy comments correctly, such as from one cell to another based on conditions:
```
compare cell values accurately and use correct Office JS APIs for comment manipulation.
```

When creating named ranges, like for the top 5 rows of a sorted range:
```
use built-in sorting methods and ensure named range covers the correct cells.
```

For filtering and sorting, such as clearing filters on a specific column or sorting by a column:
```
use specific Office JS API methods like clearFilters or sortBy, specifying the column or range as needed.
```

To handle shape insertion, like adding a square to a specific cell:
```
position shapes relative to cell positions and sizes for accurate placement.
```

For hyperlink insertion, such as creating a link with specific text:
```
use hyperlink property with address and display text, avoiding overwriting existing cell content.
```

To improve data manipulation, like copying a value from one cell to another:
```
use correct copyFrom method on destination range, ensuring data is copied from source to destination accurately.
```

For data validation and edge cases, such as limiting input to whole numbers:
```
apply data validation rules to entire ranges, using appropriate operators and formulas for restrictions.
```
