To address the failing cases, add the following rules and examples:

* When adding a formula, ensure the range is correct, e.g., "add an AVERAGE formula in cell C2 for the range B2:B8" should use `sheet.getRange("C2").formulas = [["=AVERAGE(B2:B8)"]]`.
* For copying data, specify the destination range correctly, e.g., "copy the data in columns A and B and paste it starting at column D" should use `const dst = sheet.getRange("D1"); dst.copyFrom(src, Excel.RangeCopyType.all, false, false)`.
* When freezing panes, use the correct row or column, e.g., "freeze the top row" should use `sheet.freezePanes("A1")`.
* For filtering, use real Office JS APIs, e.g., "filter the Department column to show only Engineering" should use `const filter = sheet.getRange("Department").autoFilter; filter.applyFilters([{"column": 1, "filterCriteria": {"operator": "equals", "values": ["Engineering"]}}])`.
* When updating comments, ensure the correct cell is updated, e.g., "Update the comment in cell A1" should use `const cell = sheet.getRange("A1"); cell.load("comment"); await context.sync();`.
* For copying comments, compare values correctly, e.g., "Copy the comment from cell A1 to all cells in the range B2:B5 that have the same value as cell A1" should use `const commentRange = sheet.getRange("A1"); commentRange.load("comment"); await context.sync(); const comment = commentRange.comment; const targetRange = sheet.getRange("B2:B5"); targetRange.load("values"); await context.sync(); const values = targetRange.values; for (let i = 0; i < values.length; i++) { if (values[i][0] === sheet.getRange("A1").values[0][0]) { targetRange.getCell(i, 0).comment = comment; } }`.
* When setting page layout, use correct properties, e.g., "Set the page orientation to portrait" should use `sheet.pageLayout.orientation = Excel.PageOrientation.portrait`.
* For sparklines, use correct data and sorting, e.g., "Create a sparkline in cell C2 to show the trend of values in cells A2:A20, but only for the top 5 values in
