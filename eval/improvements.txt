To address the failing cases, add the following rules and examples:

When describing a function, provide a relevant example or context specific to the user's workbook.
```javascript
// Example: Describe VLOOKUP function with a relevant example
const sheet = workbook.worksheets.getActiveWorksheet();
const exampleRange = sheet.getRange("A1:B10");
exampleRange.load("values");
await context.sync();
```

Use real Office JS APIs for filtering, such as `applyFilters` or `clearFilters`.
```javascript
// Example: Filter Department column to show only Engineering
const sheet = workbook.worksheets.getActiveWorksheet();
const table = sheet.tables.getItem("Table1");
table.columns.getItem("Department").filter.apply({ filterOn: "Engineering" });
await context.sync();
```

Check all worksheets in the workbook, not just specific ones.
```javascript
// Example: Set scale to 150% for all worksheets with 'Confidential' in cell A1
workbook.worksheets.load("items");
await context.sync();
workbook.worksheets.items.forEach(function (sheet) {
  sheet.load("name");
  sheet.getRange("A1").load("values");
  await context.sync();
  if (sheet.getRange("A1").values[0][0] === "Confidential") {
    sheet.pageSetup.scale = 150;
  }
});
await context.sync();
```

Update comments correctly, checking if a comment already exists and updating the correct cell.
```javascript
// Example: Update comment in cell A1 to include the current date
const sheet = workbook.worksheets.getActiveWorksheet();
const cell = sheet.getRange("A1");
cell.load("comment");
await context.sync();
if (cell.comment) {
  const date = new Date();
  const formattedDate = date.toLocaleDateString();
  cell.comment.text = cell.comment.text + " - " + formattedDate;
} else {
  cell.comment = { text: "New comment - " + formattedDate };
}
await context.sync();
```

Create sparklines correctly, using the right data and settings.
```javascript
// Example: Create a sparkline in cell C2 to show the trend of values in cells A2:A20
const sheet = workbook.worksheets.getActiveWorksheet();
const dataRange = sheet.getRange("A2:A20");
const sparklineRange = sheet.getRange("C2");
sparklineRange.sparklines.add(Excel.SparklineType.line, dataRange);
await context.sync();
```

Calculate moving averages and cumulative sums correctly for sparklines.
```
