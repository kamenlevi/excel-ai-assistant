To improve the handling of formulas, use the `getUsedRange` method to determine the last row with data.
```javascript
const sheet = workbook.worksheets.getActiveWorksheet();
const usedRange = sheet.getUsedRange();
usedRange.load("rowCount");
await context.sync();
const lastRow = usedRange.rowCount;
sheet.getRange(`B${lastRow + 1}`).formulas = [["=SUM(B2:B" + lastRow + ")"]];
await context.sync();
```

When setting page layout properties, use the correct units (inches, not points).
```javascript
const sheet = workbook.worksheets.getActiveWorksheet();
sheet.pageLayout.topMargin = 1; // 1 inch
await context.sync();
```
