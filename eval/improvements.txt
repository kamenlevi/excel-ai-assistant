To address the failing cases, the following rules and examples can be added:

* For [L1-questions-001], provide relevant examples: 
  When describing a function like VLOOKUP, include examples of its usage, such as `=VLOOKUP(A2, B:C, 2, FALSE)`.

* For [L1-edge-001], use Office JS APIs for filtering:
  ```javascript
  const sheet = workbook.worksheets.getActiveWorksheet();
  const filter = sheet.getRange("Department").autoFilter;
  filter.apply({ criteria: "Engineering" });
  await context.sync();
  ```

* For [L1-validation-002], set data validation for whole numbers:
  ```javascript
  const sheet = workbook.worksheets.getActiveWorksheet();
  const range = sheet.getRange("B:B");
  range.dataValidation.clear();
  range.dataValidation.add(Excel.DataValidationType.wholeNumber, {
    operator: Excel.DataValidationOperator.between,
    formula1: 1,
    formula2: 100
  });
  await context.sync();
  ```

* For [gen-L1-shapes-002], position shapes relative to the worksheet:
  ```javascript
  const sheet = workbook.worksheets.getActiveWorksheet();
  const usedRange = sheet.getUsedRange();
  const shape = sheet.shapes.addShape(Excel.ShapeType.oval, usedRange.width - 50, 0, 50, 50);
  await context.sync();
  ```

* For [gen-L2-Comments-003], correctly add comments to max value cells:
  ```javascript
  const sheet = workbook.worksheets.getActiveWorksheet();
  const used = sheet.getUsedRange();
  used.load("values");
  await context.sync();
  const rows = used.values;
  const maxRow = rows.slice(1).reduce((max, row, index) => Number(row[0]) > Number(max[0]) ? [row, index + 1] : max, [rows[1], 1]);
  const commentRange = sheet.getRange(`A${maxRow[1]}`);
  commentRange.comments.add(`Max value in column A`);
  await context.sync();
  ```

* For [gen-L2-Comments-005], update existing comments:
  ```javascript
  const sheet = workbook.worksheets.getActiveWorksheet();
  const commentRange = sheet.getRange("A1");
  commentRange.load("comments");
  await context.sync();
  if (commentRange.comments.items[0])
