When answering questions about Excel functions, provide a clear description and relevant examples, but avoid including unnecessary code.
For filtering operations, attempt to create the filter or provide an alternative solution if the specified column does not exist.
CODE_JS:
```javascript
if (!sheet.getRange("A1").getValue() === "Department") {
  sheet.getRange("A1").setValue("Department");
  const filter = sheet.getAutoFilter();
  filter.apply(filter.getRange(), "Engineering");
}
```
