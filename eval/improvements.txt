To address the failing cases, add the following rules and examples:

* When describing Excel functions, provide clear and concise explanations without unnecessary code snippets.
* Use Office JS APIs to filter data, such as `applyFilters` or `autoFilter`.
* When positioning shapes, consider the worksheet's dimensions and existing content.
* Update comments using the correct cell reference and include the current date.
* Create sparklines using the correct data source and sorting criteria.
* Use `clearFilters` with specificity to clear filters on a particular column.
* Sort data using the `sortBy` method with the correct column index and ascending/descending order.
* Use data validation to restrict input data types, such as dates or whole numbers.
* Copy and paste values using the correct `copy` and `paste` methods or the `values` property.
* Handle edge cases, such as filtering or sorting data with specific conditions.
* Create hyperlinks with display text and addresses using the `hyperlinks.add` method.
* Create named ranges with the correct range references and names.

CODE_JS examples:
```javascript
// Filter data using applyFilters
sheet.getRange("A1").autoFilter.applyFilters([{
  column: 1,
  filterCriteria: {
    operator: Excel.FilterOperator.greaterThan,
    values: [10]
  }
}]);

// Position shapes considering worksheet dimensions
const shape = sheet.shapes.addShape(Excel.ShapeType.oval, 100, 100, 50, 50);

// Update comments with current date
const date = new Date();
const dateString = date.toLocaleDateString();
cell.comment = `Updated on ${dateString}`;

// Create sparklines with correct data source and sorting
sparklineRange.sparklineGroups.add(Excel.SparklineType.line, "A2:A20", top5Values);

// Clear filters with specificity
sheet.getRange("A1").autoFilter.clearFilters();

// Sort data using sortBy method
used.sort.apply([{ key: col, ascending: true }]);

// Use data validation to restrict input data types
sheet.getRange("A1").dataValidation.add(
  Excel.DataValidationType.wholeNumber,
  {
    operator: Excel.DataValidationOperator.greaterThanOrEqual,
    formula1: 0
  },
  "Whole numbers only"
);

// Copy and paste values using correct methods
src.copy(Excel.RangeCopyType.all, false, false);
dst.paste(Excel.RangePasteType.all, false, false);

// Handle edge cases
if (cell.comment
